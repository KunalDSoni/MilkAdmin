# Origin of the NestJS API. The browser never calls this directly — Next
# proxies /bff/* to ${API_ORIGIN}/api/v1/* (see next.config.mjs), so the API's
# CORS policy never needs to change.
API_ORIGIN=http://localhost:4000
